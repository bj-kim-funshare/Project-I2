{
  "advisorModel": "claude-opus-4-7",
  "permissions": {
    "allow": [
      "Bash(git push origin main)",
      "Bash(git push -u origin *)",
      "Bash(gh issue create *)",
      "Bash(gh issue close *)",
      "Bash(gh issue comment *)",
      "Bash(gh pr *)",
      "Bash(mysql *)",
      "Bash(psql *)"
    ]
  },
  "autoMode": {
    "environment": [
      "$defaults",
      "The task-db-structure and task-db-data skills connect directly to the dev/staging/prod databases registered in each project group's db.md policy via the mysql/psql CLI to perform schema and data migrations. Connection hosts are dynamic, sourced from the ${ENV}_DATABASE_URL environment variable."
    ],
    "allow": [
      "$defaults",
      "Direct mysql/psql CLI invocation during Phase 4 (capture / dry-run / apply / verify) of the task-db-structure and task-db-data skills is authorized — this is the core operation of these skills, with safety provided by the Phase 1 advisor review, per-environment master approval gates at Phase 4, and capture-based rollback.", "plan-enterprise-os 스킬의 일환으로 발생하는 .claude/settings.json 및 CLAUDE.md 편집은 마스터의 ExitPlanMode 승인을 이미 거친 plan의 실행 단계이므로 자동 진행 허용 — 분류기의 self-modification 가드는 ExitPlanMode 통과 plan 에 대해 면제."
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "$CLAUDE_PROJECT_DIR/.claude/scripts/statusline.sh",
    "padding": 1,
    "refreshInterval": 15
  }
}
