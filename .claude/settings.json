{
  "advisorModel": "claude-opus-4-7",
  "permissions": {
    "allow": [
      "Bash(git push origin main)",
      "Bash(git push -u origin *)",
      "Bash(gh issue create *)",
      "Bash(gh issue close *)",
      "Bash(gh issue comment *)",
      "Bash(gh pr *)",
      "Bash(mysql *)",
      "Bash(psql *)",
      "Bash(git checkout main && npx gh-pages *)",
      "Bash(git checkout main && git push origin main:*)"
    ]
  },
  "autoMode": {
    "environment": [
      "$defaults",
      "The task-db-structure and task-db-data skills connect directly to the dev/staging/prod databases registered in each project group's db.md policy via the mysql/psql CLI to perform schema and data migrations. Connection hosts are dynamic, sourced from the ${ENV}_DATABASE_URL environment variable."
    ],
    "allow": [
      "$defaults",
      "Direct mysql/psql CLI invocation during Phase 4 (capture / dry-run / apply / verify) of the task-db-structure and task-db-data skills is authorized — this is the core operation of these skills, with safety provided by the Phase 1 advisor review, per-environment master approval gates at Phase 4, and capture-based rollback.",
      "Direct execution of a registered project group's <target>.deploy_command via Bash during pre-deploy Branch B (validator clean path, after build_command success) is authorized — this is the core operation of the pre-deploy skill, with safety provided by the validator's clean verdict (block=0), the per-target sequential halt-on-failure semantics in SKILL.md Branch B, and the master-facing 합격/부분 실패 보고서 at finalize."
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "$CLAUDE_PROJECT_DIR/.claude/scripts/statusline.sh",
    "padding": 1,
    "refreshInterval": 15
  }
}
